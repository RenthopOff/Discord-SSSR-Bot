const Discord = require("discord.js");

module.exports.run = async (bot, message, args) => {
  message.channel.send(`Результат: **${Math.floor(Math.random() * 2) == 0 ? "Орел" : "Решка"}**!`);
}
module.exports.help = {
  name: "coinflip",
}
